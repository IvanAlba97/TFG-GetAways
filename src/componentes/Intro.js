import '../estilos/Intro.css';

const Intro = () => {
  return (
    <div className = 'contenedor-intro'>
      <strong>GetAways</strong> es una aplicación web diseñada para aquellos amantes de la naturaleza y el senderismo, así como guardar tus rutas favoritas y compartirlas con otros senderistas. ¡GetAways es la herramienta perfecta para tus aventuras al aire libre!
    </div>
  );
};

export default Intro;